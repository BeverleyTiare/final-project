// src/services/sleepAPI.js

//  have an API endpoint for sleep data
/*const API_URL = 'https://example.com/api/sleepdata';

const sleepAPI = {
  getSleepData: async () => {
    try {
      const response = await fetch(API_URL);
      const data = await response.json();
      return data;
    } catch (error) {
      throw new Error('Failed to fetch sleep data from the API');
    }
  },
};

export default sleepAPI;*/
